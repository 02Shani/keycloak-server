// const jwt = require('jsonwebtoken');

// // export default async (req,res,next)=>{
// //     const bearerHeader = req.headers['authorization'];
// //     const token=bearerHeader && bearerHeader.splite(" ")[1];
// //     if(token === null) return res.sendStatus(401);
// //     console.log(token);
// // }